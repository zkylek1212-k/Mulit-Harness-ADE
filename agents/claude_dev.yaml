# CLI launcher 定義（怎麼起這個終端，不定義 agent loop）。由 pty handler 解析。
launcher:
  id: claude_dev
  name: Claude Code
  cli: claude
  cwd_scope: workspace
  # 把硬體分析 MCP server 掛給這個 CLI；路徑相對工作區，pty 已把 cwd 鎖在這裡
  args:
    - --mcp-config
    - .mcp/hardware.json
  env: {}
