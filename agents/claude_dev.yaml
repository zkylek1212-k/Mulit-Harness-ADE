# CLI launcher 定義（怎麼起這個終端，不定義 agent loop）。由 pty handler 解析。
launcher:
  id: claude_dev
  name: Claude Code
  cli: claude
  cwd_scope: workspace
  # 這裡刻意不預設掛任何 MCP —— 要掛什麼由 Customized 面板決定，不強加給使用者
  args: []
  env: {}
