// [Task B] STUB —— Git 中樞。用 window.api.git.*：
//   顯示 status（staged/unstaged/untracked）、log；stage/unstage/restore/commit/切分支。
//   點變更檔 → openDiff(path)（@/store）讓中央進 diff；監看 useWorkbench().gitTick 刷新。
export default function GitPanel(): JSX.Element {
  return (
    <div className="panel-stub">
      Git 狀態 / Log / Commit —— 待實作 (<code>[Task B]</code>)
    </div>
  )
}
