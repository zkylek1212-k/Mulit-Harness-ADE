// [Task C] STUB —— 預覽。activeFilePath (@/store) 為 .md → react-markdown + remark-gfm +
//   rehype-highlight + mermaid 渲染；為 .html → <iframe sandbox> 載入其內容（api.files.read）。
export default function PreviewPanel(): JSX.Element {
  return (
    <div className="panel-stub">
      Markdown / HTML 預覽 —— 待實作 (<code>[Task C]</code>)
    </div>
  )
}
