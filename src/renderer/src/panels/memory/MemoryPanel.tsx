// [Task C] STUB —— ShareProjectMem 檢視器。用 api.files.read 讀 workspace 內
//   .project-memory/handoff.md、STATE.md、DECISIONS.md（存在才顯示），markdown 渲染，
//   附重新整理鈕。找不到檔就顯示「尚未安裝 ShareProjectMem」提示。
export default function MemoryPanel(): JSX.Element {
  return (
    <div className="panel-stub">
      ShareProjectMem 檢視器 —— 待實作 (<code>[Task C]</code>)
    </div>
  )
}
