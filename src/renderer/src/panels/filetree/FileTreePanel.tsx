// [Task A] STUB —— File Tree。實作見同資料夾說明；用 window.api.files.*，點檔呼叫 openFile()（@/store）。
export default function FileTreePanel(): JSX.Element {
  return (
    <div className="panel-stub">
      File Tree —— 待實作 (<code>[Task A]</code>)
    </div>
  )
}
