# Workbench 擴充清單：唯一真相，由 Customized 面板產生各家 agent 的原生設定。
# 憑證不放這裡 —— env 值寫 ${conn:名稱}，實際值存 OS 加密儲存，spawn 時才注入。
#
# targets 可填：claude / antigravity / codex（codex 目前 pending，填了也不會產生輸出）
#
# 範例（要用時取消註解並改成你的實際指令）：
#   mcp:
#     - id: my-tool
#       command: py
#       args: ['-3', 'tools/my-tool/server.py']
#       env:
#         MY_TOKEN: ${conn:my-tool}     # 憑證在 Connections 區塊設定
#       targets: [claude, antigravity]
version: 1

skills: []

mcp: []

plugins: []
